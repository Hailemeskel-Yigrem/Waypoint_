import { describe, it, expect } from 'vitest';
import { AmenityPicker } from './AmenityPicker';

describe('AmenityPicker', () => {
  it('is a function component', () => {
    expect(typeof AmenityPicker).toBe('function');
  });
});

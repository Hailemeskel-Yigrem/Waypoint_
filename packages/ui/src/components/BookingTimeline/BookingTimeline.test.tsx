import { describe, it, expect } from 'vitest';
import { BookingTimeline } from './BookingTimeline';

describe('BookingTimeline', () => {
  it('is a function component', () => {
    expect(typeof BookingTimeline).toBe('function');
  });
});

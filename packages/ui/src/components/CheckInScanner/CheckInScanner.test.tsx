import { describe, it, expect } from 'vitest';
import { CheckInScanner } from './CheckInScanner';

describe('CheckInScanner', () => {
  it('is a function component', () => {
    expect(typeof CheckInScanner).toBe('function');
  });
});

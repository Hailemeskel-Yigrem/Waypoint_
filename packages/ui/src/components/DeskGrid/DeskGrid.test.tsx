import { describe, it, expect } from 'vitest';
import { DeskGrid } from './DeskGrid';

describe('DeskGrid', () => {
  it('is a function component', () => {
    expect(typeof DeskGrid).toBe('function');
  });
});

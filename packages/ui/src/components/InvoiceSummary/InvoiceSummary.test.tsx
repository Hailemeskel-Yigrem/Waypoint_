import { describe, it, expect } from 'vitest';
import { InvoiceSummary } from './InvoiceSummary';

describe('InvoiceSummary', () => {
  it('is a function component', () => {
    expect(typeof InvoiceSummary).toBe('function');
  });
});

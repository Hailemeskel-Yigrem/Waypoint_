import { describe, it, expect } from 'vitest';
import { PlanUsageMeter } from './PlanUsageMeter';

describe('PlanUsageMeter', () => {
  it('is a function component', () => {
    expect(typeof PlanUsageMeter).toBe('function');
  });
});

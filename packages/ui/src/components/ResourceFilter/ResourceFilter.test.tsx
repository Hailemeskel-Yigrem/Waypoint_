import { describe, it, expect } from 'vitest';
import { ResourceFilter } from './ResourceFilter';

describe('ResourceFilter', () => {
  it('is a function component', () => {
    expect(typeof ResourceFilter).toBe('function');
  });
});

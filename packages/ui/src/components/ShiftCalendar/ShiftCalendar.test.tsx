import { describe, it, expect } from 'vitest';
import { ShiftCalendar } from './ShiftCalendar';

describe('ShiftCalendar', () => {
  it('is a function component', () => {
    expect(typeof ShiftCalendar).toBe('function');
  });
});

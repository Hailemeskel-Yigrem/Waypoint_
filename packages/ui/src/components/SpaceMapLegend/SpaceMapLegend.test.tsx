import { describe, it, expect } from 'vitest';
import { SpaceMapLegend } from './SpaceMapLegend';

describe('SpaceMapLegend', () => {
  it('is a function component', () => {
    expect(typeof SpaceMapLegend).toBe('function');
  });
});

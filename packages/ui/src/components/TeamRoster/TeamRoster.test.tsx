import { describe, it, expect } from 'vitest';
import { TeamRoster } from './TeamRoster';

describe('TeamRoster', () => {
  it('is a function component', () => {
    expect(typeof TeamRoster).toBe('function');
  });
});

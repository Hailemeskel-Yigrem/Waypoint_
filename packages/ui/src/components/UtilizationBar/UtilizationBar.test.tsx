import { describe, it, expect } from 'vitest';
import { UtilizationBar } from './UtilizationBar';

describe('UtilizationBar', () => {
  it('is a function component', () => {
    expect(typeof UtilizationBar).toBe('function');
  });
});

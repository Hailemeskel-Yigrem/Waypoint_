import { describe, it, expect } from 'vitest';
import { VisitorBadge } from './VisitorBadge';

describe('VisitorBadge', () => {
  it('is a function component', () => {
    expect(typeof VisitorBadge).toBe('function');
  });
});

import { describe, it, expect } from 'vitest';
import { WebhookStatus } from './WebhookStatus';

describe('WebhookStatus', () => {
  it('is a function component', () => {
    expect(typeof WebhookStatus).toBe('function');
  });
});
